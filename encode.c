#include<stdio.h.
#include<string.h.
int main()
{
char[30],temp[30];
int spcnt[30],x,j,m;
printf("enter the string: ");
gets(string);
for(x=0;string[x];x++)
{
string[x]=string[x]+3;
}
printf("\n");
printf("enter the encoded string %s",string);
string[x]=string[x]-3;
return 0;
}
